# MindFlow Frontend Environment Variables
# In production on Vercel, set this to your deployed backend URL (e.g. Render, Railway, Fly.io)
# Example: https://mindflow-backend.onrender.com
# If left unset, frontend will default to '/api' (local Vite dev proxy)
VITE_API_URL=
